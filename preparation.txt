### -> journalizer

>> cd .ssh 
>> ssh-keygen -b 4096 -f id_rsa
>> cd PycharmProjects
>> git clone ssh://esem@git.bi.plan-net.com/srv/git/journalizer.git
>> python -m venv .venv
>> source enter_env
>> python -m pip install --upgrade pip
>> pip install -U . 
>> pip install -r requirements.txt 
>>